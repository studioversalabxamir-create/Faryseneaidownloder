# Optional AI features (install only if needed)
# Speech-to-text
faster-whisper>=1.0.3
openai>=1.51.0

# Audio separation / enhancement (heavy)
torch>=2.4.0; platform_system != 'Windows' or platform_machine != 'ARM64'
demucs>=4.0.1
openunmix>=1.3.0
pyloudnorm>=0.1.1

# NLP and recommendations
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0.post1

# Computer vision (optional thumbnail selection)
opencv-python>=4.10.0.84
